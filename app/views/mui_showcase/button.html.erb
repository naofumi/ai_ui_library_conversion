<main class="mx-auto max-w-7xl px-6 py-10" data-controller="button-preview">
  <header class="mb-8">
    <%= link_to "\u2190 Back", mui_path, class: "text-sm text-zinc-600 hover:text-zinc-900" %>
    <h1 class="mt-2 text-3xl font-semibold tracking-tight"><%= @component.page_title %></h1>
    <p class="mt-2 text-sm text-zinc-600"><%= @component.page_description %></p>
  </header>

  <section class="grid gap-6 lg:grid-cols-2">
    <article class="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm">
      <h2 class="mb-4 text-sm font-semibold uppercase tracking-wide text-zinc-500">Source (React + MUI)</h2>
      <div data-react="mui-button-demo" class="rounded-lg border border-dashed border-zinc-300 p-6"></div>
    </article>

    <article class="rounded-xl border border-zinc-200 bg-white p-6 shadow-sm">
      <h2 class="mb-4 text-sm font-semibold uppercase tracking-wide text-zinc-500">Converted (Rails + Tailwind + Stimulus)</h2>

      <!-- CODE:converted:start -->
      <div class="grid gap-4">
        <div class="mu-button-shell">
          <div class="mu-button-group">
            <button type="button" data-button-preview-target="button" class="mu-button mu-button--contained">Contained</button>
            <button type="button" class="mu-button mu-button--outlined">Outlined</button>
            <button type="button" class="mu-button mu-button--text">Text</button>
            <button type="button" class="mu-button mu-button--contained mu-button--sm">Contained / Small</button>
            <button type="button" class="mu-button mu-button--contained mu-button--lg">Contained / Large</button>
            <button type="button" class="mu-button mu-button--outlined mu-button--sm">Outlined / Small</button>
            <button type="button" class="mu-button mu-button--contained mu-button--error mu-button--sm">Error / Small</button>
          </div>
        </div>

        <div class="flex items-center gap-3">
          <button type="button" data-action="button-preview#toggleDisabled" class="mu-button mu-button--outlined mu-button--sm mu-button--utility">
            Toggle default disabled
          </button>
        </div>
      </div>
      <!-- CODE:converted:end -->
    </article>
  </section>

  <section class="mt-6 grid gap-6 lg:grid-cols-2">
    <%= render "shared/code_panel", title: "Source Code (React)", code: @source_code %>
    <%= render "shared/code_panel", title: "Converted Code (ERB)", code: @converted_code %>
  </section>
</main>
